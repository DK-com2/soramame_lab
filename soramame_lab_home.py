import streamlit as st
import datetime
import calendar
import requests
import pandas as pd
import numpy as np
import soramame

st.title("ソラマメLab")

st.write("""
### ソラマメAPIの概要

ソラマメAPIは、日本の環境省が提供するデータAPIで、各地の空気質測定データや気象データを取得することができます。このAPIを利用することで、様々な環境データにアクセスできるのですが、データ形式や取得方法が複雑であるため、扱いが難しい場合があります。

### データ解析の難しさ

ソラマメAPIから取得できるデータは、次のような理由で解析が難しいことがあります：
- データの形式が統一されていない
- 時間や日付の情報が複雑に組み合わされている
- データに欠損値や異常値が含まれていることがある

これらの要因により、データの取得と解析には専門的な知識が必要となり、一般的なユーザーには扱いにくいことが多いです。

### このサイトの目的

このサイトは、ソラマメAPIから取得したデータを簡単に扱えるようにすることを目的としています。以下のような機能を提供しています：
- データの取得を簡素化し、ユーザーが指定した条件に基づいたデータを容易に取得できるようにします。
- 取得したデータを分析しやすい形式に変換し、視覚的にわかりやすく表示します。
- ユーザーがデータのフィルタリングや加工を簡単に行えるようにすることで、データ分析の負担を軽減します。

このサイトを使用することで、ソラマメAPIのデータをより効果的に活用できるようになります。環境データの解析がより簡単に、そして直感的に行えるようにサポートします。
""")
